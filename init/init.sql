CREATE TABLE projects (
                          ID SERIAL PRIMARY KEY,
                          address TEXT NOT NULL,
                          image_path TEXT NOT NULL,
                          description TEXT
);